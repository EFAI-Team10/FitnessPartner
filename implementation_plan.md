# Fitness Partner: 실시간 자세 교정 웹 서비스 구현 계획

웹캠을 통해 사용자의 운동 모습을 실시간으로 분석하고 자세를 교정해주는 'Fitness Partner' 서비스의 초기 구현 계획입니다.

## 📌 아키텍처 및 기술 스택 제안

*   **프론트엔드 & 백엔드 (Full-stack)**: `Next.js` (React 기반). Vercel 배포에 가장 최적화되어 있으며 프론트엔드 UI와 API 라우트를 동시에 구축하기 용이합니다. *(참고: 남겨주신 답변에 'Nest.js'라고 적혀있었으나, Vercel 배포와 UI 구성을 고려할 때 프론트엔드 프레임워크인 'Next.js'를 의도하신 것으로 파악하여 이를 바탕으로 계획했습니다. 만약 순수 백엔드 프레임워크인 NestJS를 의도하신 것이라면 피드백 부탁드립니다!)*
*   **컴퓨터 비전 (자세 추정)**: `Google MediaPipe Pose`.
    *   **이유**: 브라우저 내에서 실시간(Real-time)으로 동작하며 지연 시간이 짧습니다. 비디오 데이터를 서버로 전송하지 않아 **개인정보 보호(Privacy)** 에도 유리합니다.
*   **데이터베이스 및 인증 (BaaS)**: `Supabase` (PostgreSQL 기반) 또는 `Firebase`. 
    *   로그인/회원가입, 운동 기록(개수, 무게, 자세 점수), 랭킹 시스템을 빠르게 구축하는 데 적합합니다.
*   **스타일링**: 순수 CSS (Vanilla CSS) (또는 Tailwind CSS)를 사용하여 부드러운 애니메이션과 모던한 UI(글래스모피즘, 다크 모드 등)를 구현합니다.

> [!IMPORTANT]
> **User Review Required**
> 벤치프레스의 경우, 누워있는 자세와 바벨에 의해 신체 일부가 가려질 수 있어 카메라 구도가 매우 중요합니다. 카메라를 측면 또는 비스듬히 위에서 촬영하도록 사용자 가이드를 제공하는 방향으로 진행하겠습니다.

## 🚀 구현 단계 (Proposed Changes)

### Phase 1: 프로젝트 초기화 및 인증/DB 연동
*   Next.js 프로젝트 설정 및 Vercel 배포 준비
*   Supabase (또는 Firebase) 연동 및 DB 스키마 설계 
    *   `Users` 테이블: 유저 정보 및 프로필
    *   `Workouts` 테이블: 운동 종류, 개수, 무게, 자세 점수, 날짜
*   회원가입 및 로그인 페이지 구현 (Auth)
*   아름답고 직관적인 모던 웹 UI 디자인 적용 (메인 페이지, 운동 선택 화면)

### Phase 2: MediaPipe Pose 연동
*   브라우저에 MediaPipe 라이브러리 연동
*   사용자의 웹캠 접근 권한 요청 및 비디오 스트림 화면 출력
*   웹캠 비디오 프레임을 분석하여 실시간으로 사람의 관절(Landmarks) 좌표 추출
*   화면 위에 스켈레톤(뼈대) 그래픽 오버레이 그리기

### Phase 3: 운동 자세 판별 로직 구현 (푸시업 & 벤치프레스)
*   **푸시업 (Push-up)**: 어깨, 팔꿈치, 손목의 각도 변화 및 등/허리 직선 유지 여부 판별.
*   **벤치프레스 (Bench Press)**: 팔꿈치 각도 변화, 바벨(가상의 손목 위치)의 수직 이동 경로 판별.
*   개수(Rep) 카운트 기능 및 자세의 정확도에 따른 '자세 점수' 산출 로직 추가
*   (벤치프레스의 경우) 무게 입력을 받는 UI 추가

### Phase 4: 실시간 피드백 및 랭킹 시스템 구현
*   자세가 무너졌을 때 화면에 경고 문구 및 시각적 피드백 제공
*   운동 종료 후 결과 저장 로직 구현 (개수, 무게, 자세 점수 -> DB 전송)
*   **리더보드(랭킹) 페이지 구현**: 유저들의 최고 점수, 총 볼륨(무게 x 개수) 등을 기준으로 랭킹 표시
*   최종 테스트 및 UI 폴리싱

## 🧪 검증 계획 (Verification Plan)

### 수동 검증 (Manual Verification)
*   로컬 개발 서버에서 로그인 후 웹캠을 켜고 직접 푸시업을 수행하며 카운트와 점수가 잘 반영되는지 확인합니다.
*   운동 결과가 DB에 정상적으로 저장되고, 랭킹 페이지에 반영되는지 확인합니다.
*   Vercel에 배포하여 실제 환경에서 카메라 권한 요청 및 MediaPipe 동작을 테스트합니다.
